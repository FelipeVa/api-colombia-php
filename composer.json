{
    "name": "felipeva/api-colombia-php-sdk",
    "description": "The Api Colombia PHP SDK.",
    "keywords": ["php", "api-colombia", "sdk"],
    "license": "MIT",
    "authors": [
        {
            "name": "Felipe Valencia",
            "email": "pipeedev@gmail.com"
        }
    ],
    "require": {
        "php": "^8.1.0",
        "sammyjo20/saloon": "^2.0"
    },
    "require-dev": {
        "laravel/pint": "^1.2.0",
        "mockery/mockery": "^1.5",
        "pestphp/pest": "^2.0.0",
        "phpstan/phpstan": "^1.8.6",
        "symfony/var-dumper": "^6.2.0"
    },
    "autoload": {
        "psr-4": {
            "FelipeVa\\ApiColombia\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "sort-packages": true,
        "preferred-install": "dist",
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    },
    "scripts": {
        "lint": "pint -v",
        "test:lint": "pint --test -v",
        "test:types": "phpstan analyse --ansi",
        "test:unit": "pest --colors=always",
        "test": [
            "@test:lint",
            "@test:types",
            "@test:unit"
        ]
    }
}
